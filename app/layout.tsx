import type { Metadata } from "next";
import { Manrope } from "next/font/google";
import "./globals.css";

const manrope = Manrope({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800"],
  variable: "--font-manrope",
});

export const metadata: Metadata = {
  title: "2026 Sales Tax Compliance & Technology Report | Less Taxes. More Traction.",
  description: "Download the 2026 Sales Tax Compliance & Technology Report — a vendor-agnostic guide to navigating sales tax complexity, evaluating the right tools, and turning compliance into a growth advantage.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${manrope.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
